# Pinned exactly (SECURITY_REVIEW.md, F7): these packages run with read
# access to the full Messages snapshot, so upgrades are deliberate acts —
# bump the pin, review the changelog, don't float.
anthropic==0.116.0
phonenumbers==9.0.33
pytypedstream==0.1.0
pytest==8.4.2
streamlit==1.50.0
